# All dependencies are declared in pyproject.toml.
# Install with:  pip install -e .[linux,dev]
#
# This file exists only for CI/tools that require a requirements.txt.
# It mirrors the core (non-optional) dependencies from pyproject.toml.
scapy>=2.5.0
pyyaml>=6.0
requests>=2.31.0
colorlog>=6.8.0
